


SELECT  SUM(AWBFLIGHTLINK.PCS),SUM(AWBFLIGHTLINK.GROSSWT), FLIGHTDETAILS.FLIGHT,  
FLIGHTDETAILS.FLIGHTDATE,CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1  FROM JOB, AWBFLIGHTLINK, FLIGHTDETAILS, CALLQUEUE, DRIVER 
WHERE  JOB.TENANTID=88  AND JOB.PRODUCTTYPE='FLT' AND JOB.TENANTID=FLIGHTDETAILS.TENANTID 
AND  JOB.QUEUEID= CALLQUEUE.QUEUEID  AND  JOB.FLIGHT = FLIGHTDETAILS.FLIGHT  AND JOB.FLIGHTDATE= FLIGHTDETAILS.FLIGHTDATE 
AND DRIVER.DRIVERID=CALLQUEUE.DRIVERID  AND JOB.EVENT3 IS NULL AND AWBFLIGHTLINK.FLIGHTDETAILSID = FLIGHTDETAILS.FLIGHTDETAILSID  
AND VARCHAR_FORMAT(CALLQUEUE.RTIMESTAMP,'DD-MM-YYYY')='19-08-2019' AND JOB.EVENT3 IS NULL  
GROUP BY DRIVER.NAME,FLIGHTDETAILS.FLIGHT, FLIGHTDETAILS.FLIGHTDATE, CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1 ;




SELECT SUM(AWBFLIGHTLINK.PCS),SUM(AWBFLIGHTLINK.GROSSWT), FLIGHTDETAILS.FLIGHT, 
FLIGHTDETAILS.FLIGHTDATE,CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1 
FROM  AWBFLIGHTLINK, FLIGHTDETAILS, DRIVER 
LEFT OUTER JOIN JOB ON JOB.PRODUCTID = AWBFLIGHTLINK.FLIGHT
LEFT OUTER JOIN CALLQUEUE ON CALLQUEUE.QUEUEID = JOB.QUEUEID
WHERE JOB.TENANTID=88 AND JOB.PRODUCTTYPE='FLT' AND JOB.TENANTID=FLIGHTDETAILS.TENANTID 
AND JOB.QUEUEID= CALLQUEUE.QUEUEID 
AND JOB.FLIGHT = FLIGHTDETAILS.FLIGHT AND JOB.FLIGHTDATE= FLIGHTDETAILS.FLIGHTDATE 
AND DRIVER.DRIVERID=CALLQUEUE.DRIVERID AND AWBFLIGHTLINK.FLIGHTDETAILSID = FLIGHTDETAILS.FLIGHTDETAILSID 
AND VARCHAR_FORMAT(CALLQUEUE.RTIMESTAMP,'DD-MM-YYYY')='19-08-2019'
AND JOB.EVENT3 IS NULL AND AWBFLIGHTLINK.FLIGHTDATE=FLIGHTDETAILS.FLIGHTDATE
GROUP BY DRIVER.NAME,FLIGHTDETAILS.FLIGHT, FLIGHTDETAILS.FLIGHTDATE, CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1;



**************left join petta chudu************************



/**************IDI 231 records ostunnai******************/
SELECT DISTINCT AWBFLIGHTLINK.FLIGHT,AWBFLIGHTLINK.FLIGHTDATE,AWBFLIGHTLINK.PCS,AWBFLIGHTLINK.GROSSWT,
JOB.EVENT1,CALLQUEUE.GATENO,CALLQUEUE.IDNO,CALLQUEUE.DOOR_ALLOC_TS
FROM AWBFLIGHTLINK,JOB,CALLQUEUE
WHERE AWBFLIGHTLINK.FLIGHTDATE='2019-08-19' AND JOB.PRODUCTTYPE='FLT' AND JOB.EVENT2 IS NULL AND JOB.EVENT3 IS NULL
AND JOB.TENANTID=88 AND VARCHAR_FORMAT(CALLQUEUE.RTIMESTAMP,'DD-MM-YYYY')='19-08-2019' 
AND JOB.FLIGHTDATE = AWBFLIGHTLINK.FLIGHTDATE;
/*********************************************************/


SELECT SUM(AWBFLIGHTLINK.PCS),SUM(AWBFLIGHTLINK.GROSSWT), FLIGHTDETAILS.FLIGHT, 
FLIGHTDETAILS.FLIGHTDATE,CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1 
FROM  AWBFLIGHTLINK, FLIGHTDETAILS, DRIVER 
LEFT OUTER JOIN JOB ON JOB.PRODUCTID = AWBFLIGHTLINK.FLIGHT AND JOB.FLIGHT = FLIGHTDETAILS.FLIGHT AND JOB.FLIGHTDATE= FLIGHTDETAILS.FLIGHTDATE 
LEFT OUTER JOIN CALLQUEUE ON CALLQUEUE.QUEUEID = JOB.QUEUEID AND VARCHAR_FORMAT(CALLQUEUE.RTIMESTAMP,'DD-MM-YYYY')='19-08-2019'
WHERE JOB.TENANTID=88 AND JOB.PRODUCTTYPE='FLT' AND JOB.TENANTID=FLIGHTDETAILS.TENANTID  
 AND AWBFLIGHTLINK.FLIGHTDETAILSID = FLIGHTDETAILS.FLIGHTDETAILSID 
AND JOB.EVENT3 IS NULL AND AWBFLIGHTLINK.FLIGHTDATE='2019-08-19'
GROUP BY DRIVER.NAME,FLIGHTDETAILS.FLIGHT, FLIGHTDETAILS.FLIGHTDATE, CALLQUEUE.RTIMESTAMP,FLIGHTDETAILS.STDARRIVALTIME,
DRIVER.REGNNO,CALLQUEUE.GATENO,CALLQUEUE.DOOR_ALLOC_TS,JOB.EVENT1;
